import React, { useState } from "react";

export default function UpButton() {
    const pxShow = 100;
    const [hider, setHider] = useState("hidden")//maybe? how does this work lol

    const upOnClickHandler = {
        
    }
    return (
        <button className="w-16 h-16 btn-c bg-black text-white text-center fixed right-5 bottom-10">UP</button>
    )
}